#include <stdio.h>

int main(){
    int num;
    int l1,l2,l3,l4,l5;
    scanf("%d", &num);
    l1 = num/10000;        // num/10000 ตัดหลักที่เหลือทิ้งจนเหลือหลักเดียว (12345/10000 = 1) ไม่ต้อง mod ต่อ
    l2 = (num/1000)%10;    // num/1000 ก่อน = 12 (12345/1000 ตัดหลักร้อย,สิบ,หน่วยทิ้ง) แล้ว 12%10 = 2 (ดึงหลักท้ายสุดของ 12 ออกมา)
    l3 = (num/100)%10;     // num/100 ก่อน = 123 (ตัดหลักสิบ,หน่วยทิ้ง) แล้ว 123%10 = 3 (ดึงหลักท้ายสุดของ 123 ออกมา)
    l4 = (num/10)%10;      // num/10 ก่อน = 1234 (ตัดหลักหน่วยทิ้ง) แล้ว 1234%10 = 4 (ดึงหลักท้ายสุดของ 1234 ออกมา)
    l5 = num%10;           // num%10 ดึงหลักท้ายสุดของ num ตรงๆ เลย (12345%10 = 5) ไม่ต้องหารก่อนเพราะเป็นหลักหน่วยอยู่แล้ว
    printf("%d",l3);
    printf("%d",l4);
    printf("%d",l5);
    printf("%d",l1);
    printf("%d",l2);

    // Solution two
    // int one, two, three, four, five;
    // scanf("%1d%1d%1d%1d%1d", &one, &two, &three, &four, &five);
    // printf("%d%d%d%d%d", three, four, five, one, two);
    // Explain:
    // %1d มีเลข 1 กำกับความกว้าง (field width) บอกให้ scanf อ่านแค่ 1 ตัวอักษรต่อช่องเท่านั้น
    // ต่างจาก %d ธรรมดาที่จะอ่านตัวเลขไปเรื่อยๆ จนกว่าจะเจอตัวที่ไม่ใช่เลข
    // พอจำกัดไว้แค่ 1 ตัว จึงเท่ากับ "หั่น" input เช่น 12345 ออกเป็นทีละหลักให้อัตโนมัติ โดยไม่ต้องคำนวณหาร/mod เองเหมือน Solution 1 เลย
    // -> one=1, two=2, three=3, four=4, five=5
    // พอมีตัวแปรแยกแต่ละหลักพร้อมแล้ว การสลับ "สามหลักท้าย" กับ "สองหลักแรก"
    // ก็แค่เรียงลำดับตัวแปรใหม่ตอนพิมพ์ ไม่ต้องประกอบเลขคืนด้วยการคูณ/บวกเหมือน Solution 1
    // -> พิมพ์ตามลำดับ 3,4,5,1,2 = "34512"

    return 0;
}